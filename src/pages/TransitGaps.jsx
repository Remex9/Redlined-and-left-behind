export default function TransitGaps() {
  return (
    <div className="page page-empty">
      <div className="empty-content">
        <p className="mono" style={{ color: 'var(--red)', fontSize: '0.75rem', letterSpacing: '0.1em', textTransform: 'uppercase', marginBottom: '14px' }}>
          02 - Transit Gaps
        </p>
        <p style={{ color: 'var(--gray)', fontWeight: 700 }}>
          Where transit access is weakest and which neighborhoods face longer waits, transfers, and commutes, and how today's service gaps line up with areas that were historically disinvested.
        </p>
      </div>
    </div>
  );
}
